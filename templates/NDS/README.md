# My Nintendo DS Game (template)

## Build

```bash
nix develop
make
```

This directory is the `NDS` template. When used with `nix flake new --template <repo>#NDS`
the contents of this directory will be copied into the new project.
